# Only dist/ (the bundle), bin/ (a bundled acy for platform builds), and the
# manifest belong in the .vsix.
src/**
out-test/**
node_modules/**
esbuild.mjs
tsconfig.json
tsconfig.test.json
eslint.config.mjs
.gitignore
*.vsix
dist/*.map
